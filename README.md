# 📘 Python Dictionary and JSON Handling

This project demonstrates **basic Python dictionary operations** and **JSON handling** using the built-in `json` module.  
It is beginner-friendly and helps understand how Python data can be converted to JSON, saved to a file, and read back.

---

## 🚀 What This Program Does

- Creates a Python dictionary
- Accesses and updates dictionary values
- Takes user input and stores it in a dictionary
- Converts a Python dictionary to a JSON string
- Saves JSON data into a `.json` file
- Reads JSON data back into Python
- Prints clean and formatted output

---

## 🛠️ Technologies Used

- Python 3
- JSON module (built-in)

---

## 📂 Code Overview

### 1️⃣ Create and Print a Dictionary
```python
student = {
    'name': 'pradhish',
    'age': 20,
    'mark': 75
}
print(student)
